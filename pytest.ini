[pytest]
pythonpath = .

markers =
    asyncio: async test executed by local fallback when pytest-asyncio is unavailable

filterwarnings =
    ignore:.*crypt.*deprecated.*:DeprecationWarning:passlib.*
    ignore:.*datetime.datetime.utcnow\(\) is deprecated.*:DeprecationWarning:jose.*
