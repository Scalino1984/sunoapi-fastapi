# Optional ausschließlich für lokale Stem-Erzeugung im Songstudio.
# Nicht nötig, wenn unter /admin → KI-Assistent → Stem-Erzeugung
# das Replicate-Backend ausgewählt und REPLICATE_API_TOKEN konfiguriert ist.
demucs>=4.0.1,<5
librosa>=0.10.2,<0.12
